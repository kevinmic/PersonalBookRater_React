var React = require('react');
var PropTypes = React.PropTypes;

var NabBar = React.createClass({

  render: function() {
    return (
      <div >
        <h1>NAV BAR</h1>
      </div>
    );
  }

});

module.exports = NabBar;
